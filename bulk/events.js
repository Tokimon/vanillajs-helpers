import domReady from '../domReady';
import off from '../off';
import on from '../on';
import one from '../one';
import trigger from '../trigger';

export default {
  domReady,
  off,
  on,
  one,
  trigger
};
